@{
  Layout = "_Layout";
}

<div class="page-header">
  <h1>Add a Venue</h1>
</div>
<form action="/venue-added" method="post" class="form-group">
  <label for="venue">Venue Name</label>
  <input type="text" name="venue-name" class="form-control" pattern=".{1,}" required title="One or more characters" autofocus>
  <button type="submit" class="btn btn-success form-input" name="button">Add</button>
</form>
